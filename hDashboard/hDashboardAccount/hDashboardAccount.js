if (typeof(dashboard) == 'undefined')
{
    var dashboard = {};
}

dashboard.account = {

    ready : function()
    {
        
    }
};

$(document).ready(
    function()
    {
        dashboard.account.ready();
    }
);